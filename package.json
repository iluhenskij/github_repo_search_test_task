{
  "name": "first-rpoject-node",
  "version": "1.0.0",
  "description": "wtf am i doing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7"
  },
  "scripts": {
    "serve": "browser-sync start --server --files 'dist/css/*.css, **/*.html'",
    "start": "npm run serve"
  }
}
